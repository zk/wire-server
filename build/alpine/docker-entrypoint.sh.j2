#!/bin/sh

set +e

case "$1" in
    '')
        exec /usr/bin/{{executable}} --help
        ;;

    {{executable}})
        shift
        exec /usr/bin/{{executable}} "$@"
        ;;
    
    *)
        exec "$@"
esac
