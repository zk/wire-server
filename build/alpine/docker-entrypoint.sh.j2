#!/bin/sh

set +e

case "$1" in
    '')
        exec /usr/bin/{{service}} --help
        ;;

    {{service}})
        shift
        exec /usr/bin/{{service}} "$@"
        ;;
    
    *)
        exec "$@"
esac
